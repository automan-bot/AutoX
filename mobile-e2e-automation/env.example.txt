# Appium 설정
APPIUM_URL=http://127.0.0.1:4723

# 플랫폼 선택 (android | ios)
PLATFORM=android

# 앱 경로
APP_PATH=artifacts/app-stage.apk

# 테스트 인증 정보 (절대 git에 커밋하지 말 것)
E2E_USERNAME=test_user
E2E_PASSWORD=test_password

# Android 전용 (Autox.js 바인딩)
ANDROID_SERIAL=emulator-5554

# 디바이스 팜 토큰 (선택)
DEVICE_FARM_TOKEN=

# 환경 프로파일 (dev | stage | prod)
ENV_PROFILE=stage

